{% extends 'admin/base.html.twig' %}

{% block title %}Admin - Nouvelle Séance{% endblock %}

{% block body %}
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="h3 mb-0">Créer une nouvelle Séance</h1>
        <a href="{{ path('app_admin_seance_index') }}" class="btn btn-secondary">Retour à la liste</a>
    </div>

    <div class="card">
        <div class="card-body">
            {{ include('admin/seance/_form.html.twig') }}
        </div>
    </div>
{% endblock %}
