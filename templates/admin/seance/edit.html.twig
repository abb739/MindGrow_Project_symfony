{% extends 'admin/base.html.twig' %}

{% block title %}Admin - Modifier Séance{% endblock %}

{% block body %}
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="h3 mb-0">Modifier la Séance</h1>
        <a href="{{ path('app_admin_seance_index') }}" class="btn btn-secondary">Retour à la liste</a>
    </div>

    <div class="card">
        <div class="card-body">
            {{ include('admin/seance/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        </div>
    </div>

    <div class="mt-4">
        {{ include('admin/seance/_delete_form.html.twig') }}
    </div>
{% endblock %}
